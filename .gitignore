# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
dist/
build/
*.egg-info/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# User data — should NEVER live in the repo
data/
*.brain/
.env
.env.*

# Dev-time AI-assisted build artefacts — kept locally, never published.
# Anchored to the repo root so we do NOT match the production-time
# .claude/ tree shipped inside src/remory/data_templates/.
/.claude/agents/
/.claude/commands/
/.claude/settings.local.json
/docs/phases/
